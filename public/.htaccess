Options -Indexes
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

<<<<<<< Updated upstream
  # HTTPS atrás de proxy
=======
  # Como o DocumentRoot já é /multi-menu/public, a base é só /
  RewriteBase /

  # Reconhece HTTPS mesmo atrás do proxy (Cloudflare Tunnel)
>>>>>>> Stashed changes
  RewriteCond %{HTTP:X-Forwarded-Proto} =https [OR]
  RewriteCond %{HTTPS} =on
  RewriteRule ^ - [E=IS_HTTPS:1]

<<<<<<< Updated upstream
  # Arquivo/pasta existe → serve direto
=======
  # (Opcional) Forçar HTTPS – descomente se quiser
  # RewriteCond %{ENV:IS_HTTPS} !=1
  # RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Se o arquivo ou pasta existir, serve direto (imagens, css, js)
>>>>>>> Stashed changes
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

<<<<<<< Updated upstream
  # Roteia para index.php
  RewriteRule ^ index.php [QSA,L]
</IfModule>

AddType image/webp .webp
=======
  # Senão, manda para index.php
  RewriteRule ^ index.php [QSA,L]
</IfModule>

<IfModule mod_headers.c>
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Access-Control-Allow-Origin "*"
</IfModule>

AddType image/svg+xml .svg .svgz
AddType application/javascript .js
AddType image/webp .webp

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/png "access plus 30 days"
  ExpiresByType image/gif "access plus 30 days"
  ExpiresByType image/svg+xml "access plus 30 days"
  ExpiresByType font/woff2 "access plus 30 days"
  ExpiresByType image/webp "access plus 30 days"
  ExpiresDefault "access plus 1 day"
</IfModule>
>>>>>>> Stashed changes
